{
  "name": "dailyplanner",
  "version": "1.0.0",
  "description": "Daily Planner with Clerk auth and NeonDB storage",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"node server.js\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "@clerk/express": "^1.0.0",
    "@neondatabase/serverless": "^0.10.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
